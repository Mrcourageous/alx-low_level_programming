#include <stdio.h>
/**
 * main - the code
 * the code that print numbers startingg from'0'
 * Return: 0
 */
int main(void)
{
	int zazu;

	for (zazu = 48; zazu <= 57; zazu++)
	{
		putchar(zazu);
	}
	putchar('\n');
	return (0);
}
