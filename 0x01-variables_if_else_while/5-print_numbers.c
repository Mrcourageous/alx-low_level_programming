#include <stdio.h>
/**
 * the code
 * the code that print numbers startingg from'0'
 * Return: 0
 */
int main(void)
{
	int zazu;

	for (zuzu = 48; zazu <= 57; zazu++)
	{
		putchar(zazu);
	}
	putchar('\n');
	return (0);
}
